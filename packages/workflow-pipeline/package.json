{
  "name": "@cydo/workflow-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/core/index.ts",
    "./server": "./src/server/index.ts"
  },
  "typesVersions": {
    "*": {
      "server": ["./src/server/index.ts"],
      "*": ["./src/core/index.ts"]
    }
  },
  "scripts": {
    "dev": "echo \"@cydo/workflow-pipeline has no dev server\"",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
