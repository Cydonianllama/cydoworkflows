{
  "name": "cydo-workflows",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "api": "pnpm --filter @cydo/api",
    "webapp": "pnpm --filter @cydo/webapp",
    "auth": "pnpm --filter @cydo/auth"
  }
}
